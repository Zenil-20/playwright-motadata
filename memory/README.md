# memory/ — run history + metrics store

The persistence layer (Phase 3). Schema (depth): runs, stages, gate results, metrics, cost. Start SQLite, grow to Postgres. This is what makes "continuous improvement" measurable.
