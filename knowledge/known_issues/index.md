# knowledge/known_issues

RAG corpus — known_issues. Retrieval is deterministic: structured lookup by key before any embedding search. Populate incrementally; every entry carries provenance (source + version + verified date).
