# tests/ — the runnable suite

- `regression/` — the regression specs (source of truth)
- `generated/` — tool/agent-authored suites awaiting promotion
- `scenarios/` — data-driven scenarios (one script × many rows)
- `data/` — CSV matrices that drive the scenarios
