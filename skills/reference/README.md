# skills/reference — toolkit skill guides (reference, NOT active)

Generic QA skill patterns from the toolkit. NOT synced to `.claude/skills` (only `skills/<name>/SKILL.md`
at the top level is). The active platform skills are `motadata-explorer`, `motadata-spec-writer`, `seed-data`.
