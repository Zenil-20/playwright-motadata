# agents/quality-assurance — reference personas (NOT pipeline agents)

Generic QA review lenses from the awesome-claude-code-toolkit. **These do not run the pipeline** and are
NOT synced to `.claude/` (only folder-per-agent sources with a `prompt.md` at `agents/<name>/` are).
Use them as review lenses; to activate one, copy it into `.claude/agents/`.
