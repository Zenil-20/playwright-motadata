# agents/orchestration — reference personas (NOT pipeline agents)

Generic orchestration patterns from the toolkit. Reference for the run-controller design; the real
orchestrator is `agents/orchestrator/`. Not synced to `.claude/`.
