# rules/ — platform rules

Curated coding/process rules that apply to this repo (review criteria + guardrails).
These 15 are the toolkit-imported rules (from the awesome-claude-code-toolkit), now curated as the platform's own.
